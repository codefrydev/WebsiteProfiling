/** Minimal strings catalog shape used by server-side portfolio API. */
export interface StringsCatalog {
  common: {
    emDash: string;
  };
  views: {
    home: {
      unknownBrand: string;
    };
  };
}
