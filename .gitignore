# Project (root) only. Python: src/.gitignore. React: UI/.gitignore

# WebsiteProfiling generated outputs
report.db
report.db.*

report.db-journal
nodes.json

# Optional: uncomment if input.txt has secrets
# input.txt
site_graph.svg
